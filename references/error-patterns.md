# Internal Error Patterns

이 파일은 health-analyzer.detectInternalError가 화이트리스트로 사용한다.
한 줄당 한 패턴. 빈 줄과 `#`/`==`로 시작하는 줄은 무시.

Tool result missing due to internal error
agent stopped unexpectedly
Internal server error
API Error: 500
